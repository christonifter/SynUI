% tankpath = uigetdir('D:\Synapse\Tanks');
clustpath = 'D:\Spikes\';
clustpath = 'D:\Spikes\';

[filepath, name, ~] = fileparts(tankpath);
y = dir(tankpath);
c = 0;
clear flist
for i = 3:numel(y)
    if y(i).isdir
        c = c +1;
        tanklist{c,1} = [tankpath '\' y(i).name];
        clusterlist{c,1} = [clustpath '\' y(i).name];
    end
end

